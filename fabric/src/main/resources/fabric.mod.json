{
  "schemaVersion": 1,
  "id": "rdpmouse",
  "version": "${version}",
  "name": "RDPMouse",
  "description": "Fixes mouse look when playing over RDP (Remote Desktop Protocol), and also allows camera rotation with the keyboard.",
  "license": "MIT",
  "environment": "client",
  "authors": [
    "KesslerCascade"
  ],
  "entrypoints": {
    "client": ["kesslercascade.rdpmouse.fabric.RDPMouseFabric"]
  },
  "mixins": ["rdpmouse.mixins.json"],
  "accessWidener": "rdpmouse.accesswidener",
  "depends": {
    "fabricloader": ">=0.15",
    "fabric-api": "*",
    "minecraft": ">=1.21.1"
  }
}
